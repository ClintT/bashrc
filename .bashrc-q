# ~/.bashrc: executed by bash(1) for non-login shells.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples

# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

# don't put duplicate lines or lines starting with space in the history.
# See bash(1) for more options
HISTCONTROL=ignoreboth

# append to the history file, don't overwrite it
shopt -s histappend

# for setting history length see HISTSIZE and HISTFILESIZE in bash(1)
HISTSIZE=1000
HISTFILESIZE=2000

# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize

# If set, the pattern "**" used in a pathname expansion context will
# match all files and zero or more directories and subdirectories.
#shopt -s globstar

# make less more friendly for non-text input files, see lesspipe(1)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# set variable identifying the chroot you work in (used in the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi

# set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
    xterm|xterm-color|*-256color) color_prompt=yes;;
esac

# uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
#force_color_prompt=yes

if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
	# We have color support; assume it's compliant with Ecma-48
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
	# a case would tend to support setf rather than setaf.)
	color_prompt=yes
    else
	color_prompt=
    fi
fi

if [ "$color_prompt" = yes ]; then
    if [[ ${EUID} == 0 ]] ; then
        PS1='${debian_chroot:+($debian_chroot)}\[\033[01;31m\]\h\[\033[01;34m\] \W \$\[\033[00m\] '
    else
        PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\] \[\033[01;34m\]\w \$\[\033[00m\] '
    fi
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h \w \$ '
fi
unset color_prompt force_color_prompt

# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h \w\a\]$PS1"
    ;;
*)
    ;;
esac

# enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# colored GCC warnings and errors
#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# Alias definitions.
# You may want to put all your additions into a separate file like
# ~/.bash_aliases, instead of adding them here directly.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# enable programmable completion features (you don't need to enable
# this, if it's already enabled in /etc/bash.bashrc and /etc/profile
# sources /etc/bash.bashrc).
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi

if [ -x /usr/bin/mint-fortune ]; then
     /usr/bin/mint-fortune
fi

# Android tools
export PATH=${PATH}:~/android-sdk/tools
export PATH=${PATH}:~/android-sdk/platform-tools
export PATH=${PATH}:~/.bin
export USE_CCACHE=1
export NMON=cmkn
export PATH=~/bin:$PATH
screenfetch -N
###export _JAVA_OPTIONS=-Xmx3g###
export TEMPORARY_DISABLE_PATH_RESTRICTIONS=true
export CPU_SSE42=false
alias hellojack='./prebuilts/sdk/tools/jack-admin start-server'
alias killjack='./prebuilts/sdk/tools/jack-admin kill-server'
alias startbuild='. build/envsetup.sh'
alias jack1='export JACK_SERVER_VM_ARGUMENTS="-Dfile.encoding=UTF-8 -XX:+TieredCompilation -Xmx4g"'
alias syncnow='repo sync --force-sync'
alias sync10='repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags'
alias sync1='repo sync --force-sync --current-branch --no-tags --no-clone-bundle --optimized-fetch --prune'
alias buildcleanbroken='./build-broken.sh -c1 -p bacon'
alias builddirtybroken='./build-broken.sh -c2 -p bacon'
alias resetbashrc='source ~/.bashrc'
alias havoc8='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc8'
alias havoc9='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9'
alias havoc10='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc10'
alias havocota='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc_stuff/official/android_vendor_OTA'
alias havoctarget='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/out/target/product/bacon'
alias havocofficial='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc_stuff/official'
alias broken9='export PATH=~/bin:$PATH && cd ~/Broken/Broken9'
alias corepush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/system/core && git push https://github.com/ClintT/android_system_core HEAD:havoc9'
alias corepull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/system/core && git pull https://github.com/Havoc-OS/android_system_core.git pie'
alias basepush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/frameworks/base && git push https://github.com/ClintT/android_frameworks_base HEAD:havoc9'
alias basepull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/frameworks/base && git pull https://github.com/Havoc-OS/android_frameworks_base.git pie'
alias avpull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/frameworks/av && git pull https://github.com/Havoc-OS/android_frameworks_av.git pie'
alias avpush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/frameworks/av && git push https://github.com/ClintT/android_frameworks_av HEAD:havoc9'
alias baconpush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oneplus/bacon && git push https://github.com/ClintT/device_oneplus_bacon-2 HEAD:havoc9'
alias oppopush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oppo/msm8974-common && git push https://github.com/ClintT/android_device_oppo_msm8974-common HEAD:havoc9'
alias oppopull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oppo/msm8974-common && git pull https://github.com/Feinzer/android_device_oppo_msm8974-common HEAD:aosp'
alias baconlospush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oneplus/bacon && git push https://github.com/ClintT/android_device_oneplus_bacon HEAD:havoc9'
alias baconlospull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oneplus/bacon && git pull https://github.com/Feinzer/android_device_oneplus_bacon HEAD:aosip'
alias kernelpush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/kernel/oneplus/msm8974 && git push https://github.com/ClintT/dysfunctional_kernel_oneplus_bacon HEAD:pie'
alias kernelwippush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/kernel/oneplus/msm8974 && git push https://github.com/ClintT/dysfunctional_kernel_oneplus_bacon HEAD:pie-wip'
alias settingspush='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oneplus/settings && git push https://github.com/ClintT/android_device_oneplus_settings HEAD:havoc9'
alias settingspull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc9/device/oneplus/settings && git pull https://github.com/Havoc-Devices/android_device_oneplus_settings.git pie'
alias bacon10push='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc10/device/oneplus/bacon && git push https://github.com/ClintT/device_oneplus_bacon HEAD:havoc10'
alias bacon10pull='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc10/device/oneplus/bacon && git pull https://github.com/ClintT/device_oneplus_bacon HEAD:havoc10'
alias bacon10='export PATH=~/bin:$PATH && cd ~/Havoc/Havoc10/device/oneplus/bacon'
#export LLVM_ENABLE_THREADS=true

